# Legacy E2E Tests

Status: `legacy`

Do not add new tests here. New Playwright E2E tests belong in
`tests/e2e/`.

This directory remains as a migration source for older tests and helpers. Audit
each spec before moving it because some flows overlap with newer tests in
`tests/e2e/`.

