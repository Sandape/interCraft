# Canonical E2E Tests

`tests/e2e` is the canonical Playwright test root.

Use this directory for new E2E specs, fixtures, and helpers. The root
`playwright.config.ts` points here.

Legacy tests in root `e2e/` should be migrated or deduplicated before removal.

