[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    integration: integration tests (require real DB/Redis)
    contract: API contract tests
    unit: pure unit tests
    live: live-provider tests skipped by default
filterwarnings =
    ignore::DeprecationWarning
